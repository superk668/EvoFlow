# 个人订单管理需求文档

本文档详细描述了“个人订单管理”模块的功能需求、交互流程和边界场景。

## 1. 个人中心

### 1.1 订单管理列表页

**1.1.1 进入方式**

- 用户登录后，通过点击顶部导航栏中的“我的订单”入口，进入订单管理列表页。

**1.1.2 页面元素**

- **顶部导航 (Header)**:
    - 显示当前登录用户名或手机号
    - 全局导航入口（首页、我的订单等）

- **左侧导航栏**:
    - **个人中心菜单**: 包含“我的首页”、“订单”、“我的消息”、“钱包”等入口。
    - **选中状态**: “订单”菜单项高亮。

- **右侧功能区**:
    - **通知栏**:
        - 提示用户可查看近一年订单。
        - “下载历史所有订单”链接。
    - **订单状态标签页**:
        - **全部订单**: 显示所有状态的订单。
        - **未出行 (Pending Travel)**:
            - **定义**: 订单状态为“已支付/有效”且 `出发时间 > 当前时间`。
            - **展示**: 包含即将出发的行程。
        - **待支付 (Pending Payment)**:
            - **定义**: 订单已提交但尚未完成支付（状态为 `pending_payment`）。
            - **展示**: 包含倒计时（如有）和去支付入口。
        - **待点评 (Pending Review)**:
            - **定义**: 订单状态为“已支付/有效”且 `出发时间 <= 当前时间`。
            - **展示**: 包含已完成的行程，即使后端状态仍为 `pending_travel`，前端应根据时间动态归类至此。
    - **筛选工具栏**:
        - **订单类型筛选**: 下拉选择“全部订单”、“机票”、“火车票”、“酒店”。
        - **更多筛选**: “更多筛选条件”按钮（占位）。
    - **订单列表区域**:
        - **加载提示 (Loading Indicator)**: 数据加载时的动画。
        - **错误提示 (Error Message)**: 加载失败时的重试按钮。
        - **空状态提示**: 无订单时的提示信息（“您还没有相关订单哦”）。
        - **订单卡片 (Order Card)**:
            - **选择框**: 用于批量操作。
            - **基本信息**: 订单号、下单时间、订单状态。
            - **产品信息**: 图标、标题（行程）、出发日期、出行人。
            - **金额与状态**: 订单总金额、支付状态标识。
            - **操作按钮**:
                - “查看详情”: 跳转详情页。
                - “去支付”: 跳转支付页（仅待支付状态）。
                - “取消”: 取消订单（仅待支付/未出行状态）。
                - “下载订单”: 单个导出。
    - **底部操作区**:
        - **全选**: 批量选择当前页订单。
        - **批量下载**: 下载选中订单。
        - **分页控件**: 翻页按钮和页码显示。

---

#### Scenario: 成功加载并显示全部订单列表
> 这是用户进入页面的主要正常流程。

    Given 用户已登录且拥有多个历史订单
    When 用户点击导航栏的“我的订单”
    Then 系统应跳转到订单管理列表页
    And “全部订单”标签应处于默认激活状态
    And 页面应显示一个加载提示
    And 加载成功后，系统应按订单创建时间倒序，分页展示用户的订单卡片列表

#### Scenario: 用户切换订单分类标签
> 正常流程，用户筛选不同状态的订单。

    Given 用户位于订单管理列表页
    When 用户点击“未出行”标签
    Then 系统应向服务器请求数据，并仅筛选出 `出发时间 > 当前时间` 的有效订单
    And 页面应显示一个加载提示
    And 加载成功后，列表应仅显示符合“未出行”定义的订单

#### Scenario: 查看“待点评”订单
> 正常流程，用户查看已完成行程。

    Given 用户位于订单管理列表页
    When 用户点击“待点评”标签
    Then 系统应筛选出 `出发时间 <= 当前时间` 的有效订单
    And 列表显示这些已过期的行程，状态可能显示为“待点评”或“已完成”

#### Scenario: 用户按订单类型筛选（正常流程）
> 使用“订单类型”下拉框进行类型筛选。

    Given 用户位于订单管理列表页
    When 用户在“订单类型”下拉框选择“机票”/“火车票”/“酒店”
    Then 系统应向服务器请求附带 productType 的订单数据（例如 flight/train/hotel）
    And 加载成功后，列表应仅显示对应类型的订单

#### Scenario: 切换到“待支付”标签并操作支付
> 正常流程，用户查看待支付订单并进入支付。

    Given 用户位于订单管理列表页
    When 用户点击“待支付”标签
    Then 系统应加载所有状态为 `pending_payment` 的订单
    And 每条订单应展示金额与“去支付”入口
    When 用户点击某订单的“去支付”入口
    Then 系统应跳转到支付页面（`/booking/payment/:orderId`）

#### Scenario: 用户没有登录 (状态异常)
> 边界情况，未登录用户访问。

    Given 一个未登录用户
    When 用户进入订单管理列表页
    Then 系统应显示“您还没有相关订单哦”的空状态提示
    And 不应显示订单卡片和分页控件

#### Scenario: 用户没有任何订单 (状态异常)
> 边界情况，新用户或无订单用户访问。

    Given 一个已登录但从未下过单的用户
    When 用户进入订单管理列表页
    Then 系统应显示“您还没有相关订单哦”的空状态提示
    And 不应显示订单卡片和分页控件

#### Scenario: API请求失败或网络中断 (系统异常)
> 系统异常流程，处理后端服务不可用或网络问题。

    Given 用户正在访问订单管理列表页
    When 页面在请求订单数据时，服务器返回500错误或网络连接中断
    Then 页面应停止加载动画
    And 显示一个明确的错误提示，例如：“订单加载失败，请检查您的网络并重试”
    And 提供一个“重试”按钮，允许用户手动重新加载数据

#### Scenario: 用户尝试访问一个不存在的分页 (输入异常)
> 用户通过URL或伪造请求访问一个超出范围的页码。

    Given 用户的订单共有3页
    When 用户尝试通过修改URL参数访问第4页
    Then 系统应停留在最后一页（第3页）或第一页
    And 页面内容不应发生错误，不能显示空白或崩溃

---

### 1.2 订单详情页

**1.2.1 进入方式**

- 在订单管理列表页，点击任一订单卡片上的“查看详情”按钮。

**1.2.2 页面元素**

- **返回按钮**: 返回订单列表页（通过浏览器后退或面包屑）。
- **顶部状态**:
    - 订单号显示
    - 状态文本显示：如“支付成功”、“待支付”、“待点评”、“已取消”
- **车票/产品信息卡片**:
    - 产品标题（如：城市 → 城市）
    - 航班/车次号、座席类型
    - 下单时间
- **旅客信息卡片**:
    - 乘客姓名与证件号（脱敏显示）
- **联系信息卡片**:
    - 手机号、邮箱
- **价格明细卡片**:
    - 明细项目列表（项目名、单价、数量）
    - 合计金额
- **操作区**:
    - “取消订单”按钮（仅在待支付或待出行状态下显示）
    - “重新下单”按钮
    - “下载订单”功能（通常位于列表页，详情页暂未强调）

---

#### Scenario: 成功查看可取消订单的详情
> 正常流程，查看一个允许执行操作的订单。

    Given 用户在订单列表页，且列表中有一个“待出行”的机票订单
    When 用户点击该订单的“查看详情”按钮
    Then 系统应跳转到该机票订单的详情页
    And 页面应正确显示航班号、旅客信息、价格明细等所有相关信息
    And “取消订单”按钮应处于可用状态

#### Scenario: 支付成功详情展示（正常流程）
> 展示支付成功状态。

    Given 用户已完成支付并跳转到订单详情页
    Then 页面顶部状态栏应显示“支付成功”或“待出行”
    And 右侧价格明细应正确显示票价与订单总额

#### Scenario: 已取消详情展示与重新下单入口（状态异常）
> 已取消订单的展示与操作限制。

    Given 用户查看一个已取消的订单详情
    Then 顶部状态栏应显示“已取消”
    And “取消订单”按钮不应显示
    And 页面应显示“重新下单”按钮

#### Scenario: 价格明细计算校验失败（系统异常）
> 合计金额与明细不一致时的处理。

    Given 页面展示价格明细与订单总额
    When 系统检测到明细求和不等于订单总额
    Then 页面应显示“价格明细暂不可用，请稍后重试”的错误提示
    And 允许用户继续查看非价格相关信息

#### Scenario: 用户试图访问一个不属于自己的订单 (状态异常/权限异常)
> 异常流程，防止用户越权查看他人信息。

    Given 用户A已登录
    When 用户A通过拼接URL等方式，试图访问属于用户B的订单详情页
    Then 后端应进行权限校验并拒绝请求
    And 前端页面应显示“订单不存在或您没有权限查看”的错误信息

#### Scenario: 查看一个已过期的或不可操作的订单 (状态异常)
> 边界情况，订单状态决定了可执行的操作。

    Given 用户正在查看一个“已完成”或“已取消”的订单详情
    When 页面加载完成
    Then 页面应正确显示所有订单信息
    And “取消订单”按钮不应显示

#### Scenario: 加载详情时API请求失败 (系统异常)
> 系统异常，处理后端服务或网络问题。

    Given 用户点击“查看详情”按钮
    When 详情页请求数据时，服务器无响应或返回错误
    Then 页面应显示一个覆盖性的错误提示，如：“订单详情加载失败，请稍后重试”

---

### 1.3 取消订单流程

**1.3.1 进入方式**

- 在订单详情页，点击“取消订单”按钮。

**1.3.2 页面元素**

- **确认对话框 (Browser Confirm)**:
    - 系统原生弹窗，询问“确认取消该订单吗？”
    - “确认”与“取消”选项
- **成功提示 (Alert)**: 操作成功后的弹窗提示。
- **失败提示 (Alert)**: 操作失败后的弹窗提示。

---

#### Scenario: 用户成功取消订单
> 正常流程，用户完成取消操作。

    Given 用户正在查看一个“待出行”且可被取消的订单详情
    When 用户点击“取消订单”按钮
    And 在弹出的浏览器确认框中点击“确认”
    Then 系统应向后端发送取消请求
    And 后端成功处理后，前端弹出“订单取消成功”提示
    And 页面刷新，状态更新为“已取消”

#### Scenario: 用户点击“取消”放弃操作（输入异常）
> 用户在确认弹窗中放弃操作。

    Given 取消确认弹窗已显示
    When 用户点击“取消”按钮
    Then 弹窗关闭且不发出取消请求
    And 页面保持原有状态

#### Scenario: 订单因状态限制不可被取消 (状态异常)
> 异常流程，后端校验发现订单状态已不支持取消。

    Given 用户正在查看一个“待出行”订单，但在他点击取消按钮前，该订单的状态已被系统自动变更为“已入住”
    When 用户点击“取消订单”并确认
    Then 后端服务应拒绝该请求
    And 前端应弹出相应的失败提示
    And 页面保持原状或刷新

#### Scenario: 取消过程中系统出现未知错误 (系统异常)
> 系统异常，处理后端或第三方服务失败。

    Given 用户确认取消一个订单
    When 后端在处理取消逻辑时发生错误
    Then 后端应返回一个表示系统错误的响应
    And 前端应弹出“取消失败”的提示
    And 订单状态保持不变

---

### 1.4 订单下载

**1.4.1 进入方式**

- 在订单列表页点击“下载订单”按钮。

**1.4.2 页面元素**

- **“下载订单”按钮**: 位于列表页订单卡片或操作栏。
- **文件格式**: 默认为 TXT 文本文件。

#### Scenario: 成功生成并下载订单TXT（正常流程）

    Given 用户在订单列表页
    When 用户点击某订单的“下载订单”
    Then 系统生成包含订单号、状态、金额等信息的TXT文件并开始下载

#### Scenario: 订单状态不支持下载（状态异常）

    Given 订单处于特殊状态（如数据缺失）
    When 用户点击“下载订单”
    Then 系统可能提示下载失败或无反应

#### Scenario: 文件生成失败（系统异常）

    Given 用户请求下载文件
    When 文件生成服务返回错误
    Then 页面提示“下载失败”或类似错误信息

---

### 1.5 重新下单

**1.5.1 进入方式**

- 在订单详情页点击“重新下单”按钮。

**1.5.2 页面元素**

- **“重新下单”按钮**: 仅在详情页显示。


#### Scenario: 成功跳转并预填搜索（正常流程）

    Given 用户在订单详情页
    When 用户点击“重新下单”
    Then 系统根据订单类型（如机票）提取出发地、目的地、日期等信息
    And 系统跳转到相应的搜索结果页（例如 `/flights/list?dcity=...&acity=...`）或首页预填搜索条件

#### Scenario: 跳转失败或服务不可用（系统异常）

    Given 用户点击“重新下单”
    When 搜索服务不可用或跳转失败
    Then 页面提示错误或无反应
