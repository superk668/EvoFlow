## 仅供内部查看，最终版本请删除，不用于提交
我在搞登陆模块和个人中心的时候发现了一个问题，比如我在个人中心部分，我要搞登录后的界面，就是显示用户名和和下拉菜单，但是后面出现了他搞的完全是另一个界面这样的情况，包括我之前说的注册完不跳转的情况。我认为这是不完全并行导致的。就是，其实我们让agent生成网页的逻辑和我们分工的逻辑是不能完全并行。也就是说，后面再搞复现的时候，有一些情况要对前面做更改，具体怎么更改其实很难说清。但是就记住，有些时候复现效果很差，要在前面就做更改。就比如我搞登录注册一直搞不好，就是因为，虽然我们的分工中没有首页这个模块的功能，但是首页这个模块是非常重要的。要单独分出一个模块来，后面你们的工作有涉及首页模块的也要做相应处理

感觉说了这么多还是很抽象，理解意思就行，这个还真很难说清