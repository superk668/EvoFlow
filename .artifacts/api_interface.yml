- type: Backend
  id: API-POST-AuthLoginPassword
  route: POST /api/auth/login/password
  description: 使用账号与密码登录。
  input:
    type: JSON
    body:
      account: string
      password: string
  output:
    success:
      statusCode: 200
      body:
        token: string
        userId: uuid
        userDisplayName: string
        phoneNumber: string
        loginAt: string
    error:
      - statusCode: 400
        body: { error: "Invalid input or format." }
      - statusCode: 401
        body: { error: "Invalid credentials." }
  dependencies:
    - DB-FindUserByAccount
    - DB-CreateAuthSession
  acceptanceCriteria:
    - 当账号与密码匹配时返回 200，并返回 token 与用户基础信息。
    - 当账号不存在或密码不匹配时返回 401。
  changeLog:
    - date: "2026-01-18"
      description: "初始创建。"

- type: Backend
  id: API-POST-AuthLoginSmsSend
  route: POST /api/auth/login/sms/send
  description: 为短信验证码登录发送 6 位验证码（开发期在后端终端输出）。
  input:
    type: JSON
    body:
      phoneNumber: string
  output:
    success:
      statusCode: 204
      body: {}
    error:
      - statusCode: 400
        body: { error: "Invalid phone number." }
      - statusCode: 429
        body: { error: "Too many requests." }
  dependencies:
    - DB-StoreSmsCode
  acceptanceCriteria:
    - 当手机号格式合法时生成 6 位验证码并在后端终端输出。
    - 60 秒内重复请求返回 429，不生成新验证码。
  changeLog:
    - date: "2026-01-18"
      description: "初始创建。"

- type: Backend
  id: API-POST-AuthLoginSms
  route: POST /api/auth/login/sms
  description: 使用手机号与短信验证码登录。
  input:
    type: JSON
    body:
      phoneNumber: string
      verificationCode: string
  output:
    success:
      statusCode: 200
      body:
        token: string
        userId: uuid
        userDisplayName: string
        phoneNumber: string
        loginAt: string
    error:
      - statusCode: 400
        body: { error: "Invalid phone number." }
      - statusCode: 401
        body: { error: "Invalid verification code." }
      - statusCode: 404
        body: { error: "Phone number not registered." }
  dependencies:
    - DB-FindUserByPhone
    - DB-VerifySmsCode
    - DB-MarkSmsCodeUsed
    - DB-CreateAuthSession
  acceptanceCriteria:
    - 当手机号已注册且验证码正确时返回 200，并返回 token 与用户基础信息。
    - 当验证码不正确时返回 401。
    - 当手机号未注册时返回 404。
  changeLog:
    - date: "2026-01-18"
      description: "初始创建。"

- type: Backend
  id: API-POST-AuthRegisterSmsSend
  route: POST /api/auth/register/sms/send
  description: 为手机号注册步骤1发送 6 位短信验证码（开发期在后端终端输出）。
  input:
    type: JSON
    body:
      phoneNumber: string
  output:
    success:
      statusCode: 204
      body: {}
    error:
      - statusCode: 400
        body: { error: "Invalid phone number." }
      - statusCode: 429
        body: { error: "Too many requests." }
  dependencies:
    - DB-StoreSmsCode
  acceptanceCriteria:
    - 当手机号格式合法时生成验证码并在后端终端输出，不要求检查手机号是否已注册。
    - 60 秒内重复请求返回 429。
  changeLog:
    - date: "2026-01-18"
      description: "初始创建。"

- type: Backend
  id: API-POST-AuthRegisterVerifyPhone
  route: POST /api/auth/register/verify-phone
  description: 校验注册步骤1的手机号与验证码，签发注册流程 token 进入设置密码步骤2。
  input:
    type: JSON
    body:
      phoneNumber: string
      verificationCode: string
  output:
    success:
      statusCode: 200
      body:
        registerToken: string
        phoneNumberMasked: string
    error:
      - statusCode: 400
        body: { error: "Invalid input or format." }
      - statusCode: 401
        body: { error: "Invalid verification code." }
  dependencies:
    - DB-VerifySmsCode
    - DB-MarkSmsCodeUsed
  acceptanceCriteria:
    - 当手机号格式合法且验证码正确时返回 registerToken，并允许进入步骤2。
    - 当验证码错误时返回 401。
  changeLog:
    - date: "2026-01-18"
      description: "初始创建。"

- type: Backend
  id: API-POST-AuthRegisterComplete
  route: POST /api/auth/register/complete
  description: 完成注册（设置密码并创建用户）。
  input:
    type: JSON
    body:
      registerToken: string
      password: string
      confirmPassword: string
  output:
    success:
      statusCode: 201
      body:
        userId: uuid
        phoneNumber: string
    error:
      - statusCode: 400
        body: { error: "Invalid password or mismatch." }
      - statusCode: 401
        body: { error: "Invalid register token." }
      - statusCode: 409
        body: { error: "Phone number already registered." }
  dependencies:
    - DB-FindUserByPhone
    - DB-CreateUser
  acceptanceCriteria:
    - 当密码满足 8-20 位字母/数字/符号组合且两次输入一致时返回 201。
    - 当手机号已注册时返回 409。
    - 当密码不合法或两次不一致时返回 400。
  changeLog:
    - date: "2026-01-18"
      description: "初始创建。"

- type: Backend
  id: API-POST-AuthLogout
  route: POST /api/auth/logout
  description: 退出登录并吊销当前 token。
  input:
    type: Header
    headers:
      Authorization: string
  output:
    success:
      statusCode: 204
      body: {}
    error:
      - statusCode: 401
        body: { error: "Unauthorized." }
  dependencies:
    - DB-RevokeAuthSession
  acceptanceCriteria:
    - 当用户已登录时返回 204，并使 token 失效。
    - 当 token 缺失或无效时返回 401。
  changeLog:
    - date: "2026-01-18"
      description: "初始创建。"

- type: Backend
  id: API-GET-AuthMe
  route: GET /api/auth/me
  description: 获取当前登录用户信息，用于刷新后恢复登录态。
  input:
    type: Header
    headers:
      Authorization: string
  output:
    success:
      statusCode: 200
      body:
        userId: uuid
        userDisplayName: string
        phoneNumber: string
    error:
      - statusCode: 401
        body: { error: "Unauthorized." }
  dependencies:
    - DB-FindUserByAccount
  acceptanceCriteria:
    - 当 token 有效时返回 200 与用户信息。
    - 当 token 无效时返回 401。
  changeLog:
    - date: "2026-01-18"
      description: "初始创建。"

- type: Backend
  id: API-GET-BookingDraft
  route: GET /api/booking/draft
  description: 获取当前会话中的订票草稿（用于订票/增值服务/支付/完成页一致性校验）。
  input:
    type: Header
    headers:
      Authorization: string
  output:
    success:
      statusCode: 200
      body:
        bookingDraft: object
    error:
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 404
        body: { error: "Booking draft not found." }
      - statusCode: 422
        body: { error: "Booking draft parse failed." }
  dependencies:
    - DB-GetBookingDraft
  acceptanceCriteria:
    - 当草稿存在且解析成功时返回 200 与 bookingDraft。
    - 当草稿不存在时返回 404。
    - 当草稿解析失败时返回 422。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"

- type: Backend
  id: API-PUT-BookingDraft
  route: PUT /api/booking/draft
  description: 写入/更新订票草稿（由结果页预订/订票页下一步触发）。
  input:
    type: JSON
    body:
      flightId: string
      packageId: string
      departDate: string
      priceVersion: string
      passenger: object
      contact: object
      services: array
      stage: string
  output:
    success:
      statusCode: 204
      body: {}
    error:
      - statusCode: 400
        body: { error: "Invalid input or format." }
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 500
        body: { error: "Storage error." }
  dependencies:
    - DB-UpsertBookingDraft
  acceptanceCriteria:
    - 写入时字段名必须与既定草稿结构一致。
    - 写入失败返回 500。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"

- type: Backend
  id: API-DELETE-BookingDraft
  route: DELETE /api/booking/draft
  description: 清理订票草稿（用于完成后或流程中断时回收）。
  input:
    type: Header
    headers:
      Authorization: string
  output:
    success:
      statusCode: 204
      body: {}
    error:
      - statusCode: 401
        body: { error: "Unauthorized." }
  dependencies:
    - DB-DeleteBookingDraft
  acceptanceCriteria:
    - 多次调用返回幂等成功。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"

- type: Backend
  id: API-POST-OrdersFlight
  route: POST /api/orders/flight
  description: 从订票草稿与已选增值服务生成订单并落盘到共享订单存储。
  input:
    type: JSON
    body:
      draft: object
  output:
    success:
      statusCode: 201
      body:
        orderId: string
        status: string
        expiresAt: string
    error:
      - statusCode: 400
        body: { error: "Invalid input or format." }
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 409
        body: { error: "Duplicate order create." }
      - statusCode: 500
        body: { error: "Storage error." }
  dependencies:
    - DB-CreateFlightOrder
  acceptanceCriteria:
    - 创建成功返回 201 并返回 orderId、status=pending_payment、expiresAt（15分钟倒计时基准）。
    - 当检测到重复创建（同一会话已创建 createdOrderId）返回 409。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"
    - date: "2026-01-19"
      description: "调整请求体字段为 draft，与前端实现保持一致。"

- type: Backend
  id: API-GET-Orders
  route: GET /api/orders
  description: 查询订单列表（默认查询机票订单）。
  input:
    type: Query
    query:
      productType: string
      tab: string
      page: number
      pageSize: number
  output:
    success:
      statusCode: 200
      body:
        orders: array
    error:
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 429
        body: { error: "Too many requests." }
      - statusCode: 503
        body: { error: "Service busy." }
      - statusCode: 422
        body: { error: "Orders parse failed." }
  dependencies:
    - DB-ListOrders
  acceptanceCriteria:
    - 支持按 productType 筛选（all/flight/train/hotel）。
    - 支持按 tab 筛选（all/pending_payment/pending_travel/pending_review）。
    - 支持分页 page/pageSize，后端对 pageSize 上限强校验。
    - 解析失败返回 422。
    - 当触发限流或服务繁忙时返回 429/503。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"
    - date: "2026-01-19"
      description: "补充分页与 tab/productType 筛选参数。"
    - date: "2026-01-19"
      description: "补充限流/服务繁忙错误码，用于前端降级提示与重试。"

- type: Backend
  id: API-GET-OrderById
  route: GET /api/orders/:orderId
  description: 获取订单详情（用于订单详情页/支付页渲染）。
  input:
    type: Path
    params:
      orderId: string
  output:
    success:
      statusCode: 200
      body:
        order: object
    error:
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 404
        body: { error: "Order not found." }
      - statusCode: 422
        body: { error: "Order parse failed." }
  dependencies:
    - DB-GetOrderById
  acceptanceCriteria:
    - 订单存在时返回 200。
    - 订单不存在返回 404。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"

- type: Backend
  id: API-PATCH-OrderStatus
  route: PATCH /api/orders/:orderId/status
  description: 更新订单状态（完成页转为 pending_travel，订单中心支持取消）。
  input:
    type: JSON
    params:
      orderId: string
    body:
      status: string
  output:
    success:
      statusCode: 204
      body: {}
    error:
      - statusCode: 400
        body: { error: "Invalid status." }
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 404
        body: { error: "Order not found." }
      - statusCode: 403
        body: { error: "Forbidden." }
      - statusCode: 500
        body: { error: "Storage error." }
  dependencies:
    - DB-UpdateOrderStatus
  acceptanceCriteria:
    - status=pending_travel 用于完成页推进订单状态。
    - status=canceled 用于订单中心取消订单。
    - 更新成功返回 204。
    - 状态不合法返回 400，状态不允许变更返回 403。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"
    - date: "2026-01-19"
      description: "调整为仅依赖路径参数 orderId，适配完成页无请求体调用。"
    - date: "2026-01-19"
      description: "增加请求体 status 以支持取消订单与通用状态变更。"

- type: Backend
  id: API-POST-OrderPay
  route: POST /api/orders/:orderId/pay
  description: 模拟支付下单（校验倒计时、卡信息与乘机人一致性）。
  input:
    type: JSON
    body:
      payMethod: string
      newCard: object
  output:
    success:
      statusCode: 200
      body:
        paidAt: string
        nextRoute: string
    error:
      - statusCode: 400
        body: { error: "Invalid input or format." }
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 404
        body: { error: "Order not found." }
      - statusCode: 409
        body: { error: "Payment expired." }
      - statusCode: 422
        body: { error: "Passenger info inconsistent." }
      - statusCode: 500
        body: { error: "Payment failed." }
  dependencies:
    - DB-GetOrderById
    - DB-GetBookingDraft
  acceptanceCriteria:
    - 倒计时到期返回 409。
    - 乘机人与联系人信息缺失或与草稿不一致返回 422。
    - 成功支付返回 200 且 nextRoute 指向 /buy-ticket/step4 或 /booking/complete。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"
    - date: "2026-01-19"
      description: "移除请求体中的 orderId 字段，订单号由路径参数提供。"

- type: Backend
  id: API-GET-OrderExportTxt
  route: GET /api/orders/:orderId/export/txt
  description: 导出单个订单 TXT 内容（用于列表/详情页下载）。
  input:
    type: Path
    params:
      orderId: string
  output:
    success:
      statusCode: 200
      body:
        filename: string
        content: string
    error:
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 404
        body: { error: "Order not found." }
      - statusCode: 500
        body: { error: "Generate failed." }
  dependencies:
    - DB-GenerateOrderTxt
  acceptanceCriteria:
    - 成功时返回 200，filename 默认为 order-:orderId.txt。
    - 当订单不存在时返回 404。
    - 当生成失败时返回 500。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"

- type: Backend
  id: API-POST-OrdersExportTxt
  route: POST /api/orders/export/txt
  description: 批量导出订单 TXT 内容（用于批量下载/历史订单下载）。
  input:
    type: JSON
    body:
      orderIds: array
  output:
    success:
      statusCode: 200
      body:
        filename: string
        content: string
    error:
      - statusCode: 400
        body: { error: "Invalid input or format." }
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 422
        body: { error: "Orders parse failed." }
      - statusCode: 500
        body: { error: "Generate failed." }
  dependencies:
    - DB-GenerateOrdersTxt
  acceptanceCriteria:
    - 当 orderIds 为空数组时导出近一年全部订单。
    - 当 orderIds 非空时仅导出指定订单集合。
    - 解析失败返回 422。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"

- type: Backend
  id: API-POST-UserCenterMyInfo
  route: POST /api/user-center/my-info
  description: 同步“我的信息”编辑结果到后端（本地已保存后可失败兜底）。
  input:
    type: JSON
    body:
      nickname: string
      name: string
      gender: string
      birthday: string
      profileVersion: string
  output:
    success:
      statusCode: 204
      body: {}
    error:
      - statusCode: 400
        body: { error: "Invalid input or format." }
      - statusCode: 401
        body: { error: "Unauthorized." }
      - statusCode: 409
        body: { error: "Profile conflict." }
      - statusCode: 500
        body: { error: "Server error." }
  dependencies:
    - DB-UpdateUserProfile
  acceptanceCriteria:
    - nickname 必填且长度不超过 20 字符；name 必填且长度不超过 30 且不含数字/特殊字符；gender 为 男/女；birthday 为空或 yyyy-MM-dd 且不得为未来日期。
    - 当请求字段校验通过且更新成功时返回 204。
    - 当 profileVersion 存在且版本不一致时返回 409，且后端不落库该次更新。
  changeLog:
    - date: "2026-01-19"
      description: "初始创建。"
    - date: "2026-01-19"
      description: "补充 profileVersion 用于 409 冲突检测，并对齐字段校验规则。"
